#include <bits/stdc++.h>

#include <atcoder/all>
#define rep(i, n) for (int i = 0; i < n; i++)
using namespace std;
using namespace atcoder;
using ll = long long;

int main() {
  double n;
  cin >> n;

  cout << setprecision(10) << (9.0 / 5.0 * n) + 32 << endl;
  return 0;
}