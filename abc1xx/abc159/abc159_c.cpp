#include <bits/stdc++.h>

#include <atcoder/all>
#define rep(i, n) for (int i = 0; i < n; i++)
using namespace std;
using namespace atcoder;
using ll = long long;

int main() {
  double L;
  cin >> L;

  cout << fixed << setprecision(20) << (L / 3) * (L / 3) * (L / 3) << endl;
  return 0;
}